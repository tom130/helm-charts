apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ca-whatsapp-frontend-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ca-whatsapp-frontend-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ca-whatsapp-frontend-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
